syntax = "proto3";

package myrpc;

message RpcHeader {
    string service_name = 1;
    string method_name = 2;
    uint32 args_size = 3;
}

message RpcResponseHeader {
    int32 error_code = 1;
    string error_text = 2;
    uint32 response_size = 3;
}

